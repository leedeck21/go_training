# api

This folder contains API definitions, handlers, and related code. It is typically used for REST, gRPC, or other service interface definitions and their implementations.

**Overview:**

- API route handlers
- Protocol definitions (REST, gRPC, etc.)
- API documentation
